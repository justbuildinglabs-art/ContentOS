# ContentOS v0.1.0: decisions made during implementation

Every ruling the controlling session made on Leslie's behalf while executing the plan on 2026-09-16 and 2026-09-17, in order. Each line ends with what it costs if the ruling was wrong. Revisit any of them by editing the code or the spec; nothing here is load-bearing beyond what the tests pin.

- | Tasks | Produces vs consumes | Finding | Ruling |
- | 4 ↔ 12 | env.diagnose ffmpeg flag vs frames.ffmpeg_available | both use shutil.which | Ruling: Task 12 may import env or call shutil directly — no conflict — cost if wrong: none |
- | 4 ↔ 7 | diagnose --live needs apify.check_token | check_token lands in Task 7 | Ruling: --live wired in Task 7, Task 4 ships diagnose without it — cost if wrong: none |
- | 10 ↔ 11,12 | research writes video_status/frames_status before download/frames exist | ordering gap | Ruling: Task 10 writes "pending"; Tasks 11/12 rewrite statuses and wire into research — cost if wrong: one extra rewrite |
- | 11 ↔ 12 | fixtures/sample.mp4 needed by both | plan had Task 12 creating it after Task 11 used it | Ruling: Task 11 creates sample.mp4 + sample_cover.jpg; Task 12 adds fixtures/frames/sample/*.jpg — cost if wrong: none |
- | 2 ↔ 15 ↔ 16 | formats.md table vs coverage test vs word_budget_for parser | header must be exactly `| format | target_length_s | word_budget | beats |` | Ruling: header fixed in Task 2 brief — cost if wrong: parser edit |
- | 2 ↔ 16 | examples/*.md written before verify_script exists | verifier may be stricter than the author's reading | Ruling: Task 16 may edit the examples to satisfy the verifier without changing their substance — cost if wrong: none |
- | 19 ↔ 3+ | e2e runs main() in-process under the no-network patch | subcommands must return codes, not call sys.exit | Ruling: main() returns int; only the __main__ guard exits — applies from Task 3 — cost if wrong: test harness edit |
- ## Rulings (setup)
- Ruling: ContentOS is its own git repository; the parent ~/git had an accidental empty .git (unborn master, zero commits) that made ContentOS look like a subdirectory of it. I restored the parent to its untouched state and did not delete it — cost if wrong: none (Leslie can remove ~/git/.git if it was accidental).
- Ruling: Task 1 (spec + scaffold) performed by the controller during setup because the repo had no commits and the spec is a mechanical extraction of the approved plan — cost if wrong: one unreviewed scaffold commit.
- Ruling: implementation on branch contentos-v1 in place, no separate worktree directory: the repo was empty (nothing to protect), the files should stay visible in Leslie's project pane, and worktree consent could not be obtained mid-run — cost if wrong: none material, branch isolation still holds.
- Ruling: tasks renumbered 1–19 with '### Task N' headings so the brief extractor works; plan file updated — cost if wrong: none.
- Ruling: http download status mapping 410 → expired, 403 → blocked, 404/other 4xx → failed — cost if wrong: a status label.
- Ruling: pinned reels are dropped during normalization (they bias medians; the actor input already skips them) — cost if wrong: a few missed candidates.
- Ruling: examples keep exactly the seven contract sections and no extra heading; attribution moves to a trailing HTML comment `<!-- Sources: ... -->`; Task 15's Sources-footer test covers hooks.md, formats.md, scripting.md, qa-rubric.md only — cost if wrong: one test edit.
- Ruling: word counts exclude bracketed markers (`[PAUSE]`, `[EMPHASIS]`, `[NEED ...]`) and never count the `[VISUAL CUE]` column; stated in formats.md and qa-rubric.md now and applied by Task 16's verifier — cost if wrong: a few words of budget drift.
- Ruling: env.diagnose takes skill_root as a parameter from the CLI (no import of contentos.py from lib, avoiding a cycle); the global config dir derives from environ["HOME"] so tests can redirect it — cost if wrong: a signature change.
- Ruling: stub handlers carry an `is_stub = True` attribute; the test becomes `test_every_subcommand_is_registered_and_stubs_exit_1` (every subcommand accepted by the parser, stub-flagged ones exit 1 with "not implemented"), so it adapts as tasks wire handlers. Scope of Task 4 expanded to tests/test_cli.py and the stub factory; fixed before review — cost if wrong: one test edit.
- Ruling: a missing HOME (and no CONTENTOS_CONFIG_DIR) means "no global config file", the same as CONTENTOS_CONFIG_DIR=""; resolve_keys never raises on environment shape; add a test for it — cost if wrong: none.
- Ruling: load_config preserves unknown keys (forward compatible) and ensure_gitignore appends missing lines to an existing .contentos/.gitignore rather than overwriting founder edits — cost if wrong: none.
- Ruling: on collision init_run keeps the mandated id format and appends -2, -3, … (up to -99, then raises); the suffixed id is what run.json records and what "latest" resolves, since it sorts after the bare id and before the next second — cost if wrong: an id suffix nobody sees.
- Ruling: collision suffixes are zero-padded to two digits (-02 … -99) so lexical and numeric order agree — cost if wrong: none.
- Ruling: request_json sends `User-Agent: contentos-plugin` unless the caller overrides it; the browser UA and Instagram referer are for downloads only; `retries` counts additional attempts after the first, backoff 2/4/8 s reused past the third — cost if wrong: header text.
- Ruling: check_token returns False on any HTTPError or URLError (never raises) and diagnose always carries `apify_live` (null without --live, bool with it) so the JSON shape is stable — cost if wrong: one key.
- Ruling: check_token catches http.HTTPError and OSError (URLError and socket.timeout are OSError subclasses) and returns False; pre-review fix — cost if wrong: none.
- Ruling: fixture reel timestamps sit in Aug–Sep 2026 except one dated April 2026; tests and mock runs that need "now" pass 2026-09-16 UTC explicitly so lookback math is deterministic — cost if wrong: fixture edits.
- Ruling: error items are attributed by exact, case-insensitive match of the inputUrl's last non-empty path segment (with any leading @ stripped) — cost if wrong: a status label.
- Ruling: numeric fields accept numeric strings and treat anything unparseable as missing; non-dict comment entries are skipped; one malformed field never aborts normalization — cost if wrong: none.
- Ruling: the viral_proof formula is authoritative; the spec's worked example "ratio 3 → 4.0" is an approximation of 2.5·log2(3) = 3.96 (8 → 7.5 and 16 → 10 are exact); tests assert 3.96, and the spec text gets corrected in the finishing pass — cost if wrong: none.
- Ruling: reels on a likes-metric baseline are scored and flagged but never selectable (selection requires plays); exclusion reasons apply in the order no_plays, no_baseline, outside_lookback, below_min_plays, per_account_cap — cost if wrong: reason labels.
- Ruling (reaffirmed): likes-baseline reels are scored and flagged but never selectable; codify with a docstring note and a pinning test in fix round 1 — cost if wrong: none.
- Ruling: run_research signals exit conditions with typed ResearchError subclasses carrying exit_code and an optional JSON payload; the CLI handler maps them — cost if wrong: handler shape.
- Ruling: in mock mode "now" defaults to 2026-09-16T00:00:00Z so mock runs stay reproducible as the fixtures age; live mode uses the real clock — cost if wrong: none.
- Ruling: fixtures for mock mode are located at plugin_root/fixtures (Path(__file__).resolve().parents[4] from lib/research.py) — cost if wrong: a path constant.
- Ruling: the registration check uses `<subcommand> --help` (exit 0 proves the parser knows it); stubs are still run bare and must exit 1 with "not implemented"; non-stubs are not run bare. Scope of Task 10 expanded to tests/test_cli.py; fixed before review — cost if wrong: one test edit.
- Ruling: resuming a run with no recorded Apify runs raises a typed NothingToResume(ResearchError) with exit 2 and a message telling the founder to start a new research run — cost if wrong: an exit code.
- Ruling: a reel whose download fails stays listed in `selected` with its failure status (visible to the founder) and one backfill reel is promoted per failure while the pool lasts; downstream stages act only on video_status == ok — cost if wrong: list bookkeeping.
- Ruling: cover download failure never fails a reel (logged, cover_status recorded) — cost if wrong: none.
- Task 11: review (cfe9901..6c25f13) — Approved, spec compliant. Minor (deferred): no test for a backfill chain deeper than one promotion; research's top-level status ignores video warnings. Ruling: the test_research.py edit adding no_download=True to test_mock_research_writes_01_02_and_run_json is approved (the new default-on download would otherwise flip its pending assertions) — cost if wrong: none.
- Ruling: frame times = fixed 0,1,2,3 s (those below duration − 0.5) then the remaining slots spread evenly up to duration − 0.5 inclusive, rounded to 2 decimals; unknown duration uses the fixed points only — cost if wrong: frame positions.
- Ruling: the scale filter never upscales: `scale='if(gt(iw,ih),min(iw,L),-2)':'if(gt(iw,ih),-2,min(ih,L))'` passed as one argv element without a shell; reels without an ok video get frames_status "no_video" — cost if wrong: image size.
- Ruling: schemas do not use additionalProperties; the validator ignores unknown keys and coerce_analysis drops them; unknown emotion_lead coerces to curiosity, unknown confidence to low, missing hook_seconds to 3.0 — cost if wrong: coercion defaults.
- Ruling: the director prompt derives frame timestamps by frame_times(duration_s, number of frame files present); briefs.md headings use "## B01: title" (no em dashes in founder-facing text) — cost if wrong: none.
- Task 13: implementer DONE_WITH_CONCERNS (commits 61c6480..e2cd015, 179/179 on both interpreters). Ruling: rank_briefs takes a fourth parameter run_dir (needed for absolute frames_dir/analysis_path); Task 14 calls rank_briefs(analyses, reels, n, run_dir) — cost if wrong: none. Notes: no qa convenience wrappers (validate_against(load_schema("qa"), obj) serves Task 16); scoring.md path referenced before Task 15 creates it. Review dispatched.
- Ruling: frame timestamps are keyed by each file's parsed index against frame_times(duration_s, highest index present); indices beyond the computed list get "time unknown" — cost if wrong: prompt labels.
- Ruling: command-level logic for direct-prompt, synth-prompt, verify (direct|synth), and rank lives in a new lib/direct.py (director.py is already large); the verify subparser accepts --stage direct|synth|write|qa with write/qa stubbed until Task 16 — cost if wrong: a module name.
- Ruling: verify --stage direct coerces first, validates the coerced object, then also fails on empty brief_title/why_it_worked/transferable_mechanism/adaptation_for_product/avoid or an empty structure, rewrites the coerced JSON in place, and records analysis_status on the reel in 02-outliers.json — cost if wrong: a stricter gate.
- Ruling: the longer description is ratified (it covers synthesis mode); the test pins it. The prose minors (missing-reference fallback, cover-only structure guidance, headings one per line), the fixture wording, one cover_only test, and catching ValueError on JSON reads are folded into fix round 1 because they shape first-run output quality — cost if wrong: one extra round.
- Ruling: scoring.md states the viral_proof table as 3 → 3.96, 8 → 7.5, 16 → 10 and notes that the spec's "4.0" is an approximation; the Sources-footer test covers the seven reference files and excludes examples/ — cost if wrong: none.
- Ruling: the scoring.md sentence is corrected in the same fix round (the director reads that file) — cost if wrong: none.
- Ruling: the script word count is the Beats table's spoken/VO and on-screen text columns with bracketed markers removed; over budget by more than length_tolerance is an error, under budget by more than the tolerance is a warning only; read time is spoken words divided by 2.5 — cost if wrong: a verifier constant.
- Ruling: an extra `## ` heading in a script (for example `## Sources`) is an error; `[NEED ...]` placeholders are collected, never errors; a `pass` QA verdict is rejected by verify_qa when any check fails, any score is below the threshold, length is out of tolerance, or confidence is below the threshold — cost if wrong: a stricter gate.
- Task 16: implementer DONE_WITH_CONCERNS (commits 391fb5b..85e9c36, 247/249): Task 14's test_verify_write_and_qa_stages_are_still_stubs now fails by design. Ruling: replace it with test_verify_write_and_qa_stages_are_wired (exit 2 for a brief with no script or QA file, no "not implemented" text); scope expanded to tests/test_direct_commands.py; fixed before review. Ruling: write_prompt/qa_prompt take a cfg keyword (needed for length_tolerance) — cost if wrong: none. The implementer's spawn_task chip was dismissed as handled here.
- Ruling: agent descriptions are `Writes one Instagram Reel script from a ContentOS brief, or revises it from QA findings. Dispatched by /contentos; not for direct use.` and `Reviews one ContentOS Reel script against its brief, the product facts, and the QA rubric, and writes a QA verdict JSON. Dispatched by /contentos; not for direct use.`; tests pin them — cost if wrong: a string.
- Ruling: the prose minors that shape first-run output (Beats lead-in warning, QA missing-reference fallback, founder rules in QA, empty product.md section → [NEED ...] placeholder, rubric authority scoped so one-watch findings become issues rather than an automatic revise, read inputs in as few calls as possible) and the two test-hardening minors are folded into fix round 1 — cost if wrong: one extra round.
- Ruling: the skill never pastes generated prompts through its own context; it redirects each `*-prompt` command's stdout to `<run_dir>/prompts/<name>.md` and dispatches the subagent with a two-line prompt pointing at that file; each agent file gains one sentence allowing "a dispatch that points at a prompt file" (scope of Task 18 includes that sentence and its test) — cost if wrong: prompt plumbing.
- Ruling: AskUserQuestion allows at most four options, so brief selection offers `All N`, `Top 3`, `Top 1`, and free text ids via Other; the setup interview is conversational (three short rounds of plain questions) and AskUserQuestion is used only for choices (cost confirmation, overwrite, brief selection) — cost if wrong: UX wording.
- Ruling: setup answers gain an optional product_name; unanswered template sections keep their guidance line plus `TODO: fill this in.` — cost if wrong: none.
- Ruling: the README's cost line stays at the accurate $0.84 for ten accounts at 30 reels (the controller's "$0.09" was an arithmetic error: that figure is three accounts at ten reels); the spec's cost line is corrected in the finishing pass and the error is reported to Leslie — cost if wrong: none.
- Ruling: --force preserves an existing valid config.json and replaces only `competitors` (founder-tuned settings survive); SKILL.md's confirmation says exactly that — cost if wrong: a merge rule.
- Ruling: handles must match [A-Za-z0-9._]+ after normalization, otherwise setup exits 2 naming the entry; `runs/*/prompts/` joins the .contentos/.gitignore lines and the README tree; SKILL.md clarity items (run_dir from RESULT, --run latest, mock note placement, appending verify errors with `cat >>`) are folded into fix round 1 because they change runtime behavior — cost if wrong: one extra round.
- Ruling (parked): the spec's Reference files table still says gold scripts live at references/examples/<format>.md and the .contentos tree omits examples/; README and the code are correct (project copy wins, plugin copy is the fallback); no second fix wave — cost if wrong: a stale spec line.
- Ruling (parked): tests/test_skill_md.py:288 still asserts the string "references/examples/" in README, which now passes only because README says not to use that directory; behavior is covered by the two write_prompt tests — cost if wrong: a misleading assertion name.
- Ruling (parked): PEP 8 double blank lines inside three test classes; cosmetic.
